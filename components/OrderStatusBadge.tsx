import { Camera } from "lucide-react";

export default function OrderStatusBadge(order_status_id: string) {
  const Badges = [
    {
      status: "created",
      icon: Camera,
      color: "green",
    },
  ];
  return <></>;
}
